# Reproducible runtime for the full causal experiment. Install in a clean venv:
#   python -m pip install -r requirements.txt
# WSL's configured package index currently publishes NumPy through 2.2.6.
# This explicit pin is compatible with Python 3.10 and the packages below.
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
statsmodels==0.14.5
pmdarima==2.1.1
arch==7.2.0
openpyxl==3.1.5
# CPU wheel avoids pulling several GB of CUDA libraries on the WSL environment.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
xgboost==3.0.4
lightgbm==4.6.0
vmdpy==0.2
EMD-signal==1.6.4
matplotlib==3.10.7
requests==2.32.5
# pytrends 4.9.2 still uses urllib3's pre-2.0 retry API.
urllib3==1.26.20
yfinance==0.2.66
pytrends==4.9.2
textblob==0.20.1
# FinBERT robustness scoring for lithium-specific headlines.
transformers==5.15.0
wordcloud==1.9.4
python-docx==1.1.2
pytest==8.3.5
